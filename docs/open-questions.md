# Open Questions

Все вопросы ниже требуют явного решения и не должны закрываться молчаливыми допущениями.

## Закрыто (подтверждено пользователем, 2026-03-27)
1. При конфликте источников важнее примечания в реестре.
2. Журнал используется для сверки дат; отклонения 1-2 дня допустимы, если нет явных нестыковок.
3. Дата окончания работ не должна быть позже закрытия.
4. Форматы дат в документах могут отличаться по месту; ориентир — формат конкретного поля по примерам.
5. Именование акта: `№<номер>-<система>_<секция>.docx` (обязательный символ `№` в начале).
6. Для приложений/испытаний/промывки и т.п. добавляется соответствующая приписка в конце имени файла.
7. Состав обязательных актов определяется реестром; от реестра не отклоняться, кроме исправления явной грамматической опечатки.
8. Сертификаты обрабатываются по реестру; дополнительные сертификаты в папке не являются входом для формирования.
9. Правило приложения жёсткое: если сертификатов 6 и более — отдельное приложение; если 5 и меньше — реквизиты в акте.
10. Допустим единый рабочий табличный контур, но с разбиением по секциям сверху вниз в соответствии с реестрами.
11. В текущем этапе помощь по рисованию схем не требуется.
12. Выбор стека для будущей реализации: Python.
13. Исходные акты в `inputs/` полностью read-only: не изменяем содержимое и не переименовываем файлы.
14. Эталон для названия объекта, ФИО подписантов, номеров приказов и реквизитов организаций — существующие акты `Промывка` и `Дезинфекция`; АОУСИТО для этого не используется.
15. Основная рабочая таблица ведётся отдельно от журнала работ: журнал остаётся самостоятельным источником для будущего заполнения дат. Рабочую таблицу хранить вне `inputs/` как новый производный артефакт (подтверждено пользователем, 2026-03-27).
16. Все проверки/переименования готовых актов выполнять только на копиях в `output/`.
17. В `output` не дублировать папку `shared` из `inputs`; формировать только рабочую структуру по секциям с актами.
18. Если акту не нашлась пара в реестре (лишний акт), в имени ставить префикс `! ` и далее номер акта из самого файла (с fallback на имя файла), при возможности добавлять распознанный тег типа акта.
19. Для дублей одного номера: номер реестра остаётся у акта, совпадающего по заголовку с реестром; дополнительный акт включается как `№<номер>.<суффикс>-...` (например, `№6.1-...`), добавляется отдельной строкой в `Реестр (акты)` под базовым номером и подсвечивается ярко-синим.

## Приоритет Low
1. Нужны ли дополнительные проверки на служебные временные файлы (`~$`, `.DS_Store`) в будущей автоматизации?

2. Нужен ли единый словарь синонимов по материалам/оборудованию для устранения расхождений в названиях?

## Приоритет High
1. Зафиксированы расхождения между именем файла и содержимым готовых актов в `inputs/vk/projects/kosmonavtov-12/sections/*/ready-acts/`:
- `12.1/№5-В2 Протокол испытания  сплинкеры.doc` и `12.1/№7-В2 Протокол испытания ВПВ.doc` внутри содержат номер `№6-В2/1`.
- `12.2/№5-В2 Протокол испытания  сплинкеры.doc` и `12.2/№7-В2 Протокол испытания ВПВ.doc` также внутри содержат номер `№6-В2/1` (номер от секции `12.1`).
- Файлы `АОУСИТО для пожарника` с агрегированными системами (`№1-В2.1,В2.2,...`) не имеют прямого 1:1 соответствия строкам реестра актов.
2. Требуется решение, что считать истиной при проверке `готовности акта`: имя файла, номер/заголовок внутри файла, либо строгое соответствие строке реестра.

## Правило ведения
- Каждый вопрос закрывается только после явного подтверждения пользователя.
- После закрытия вопроса обновляются `docs/` и `sections/VK/`.
